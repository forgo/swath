# Swath — Performance

_Measured, not asserted. Every number in this document was produced by a named `just` recipe on a
stated machine at a stated commit, is diffable against a committed artifact under `docs/perf/`, and
was re-measured for this document — no figure is inherited from an earlier writeup without being
re-run. Where a historic claim did not reproduce as stated, the honest number is here instead._

## Measurement stamp

Every figure below traces to one of these artifacts. The sha is the commit the measurement ran on;
`just perf-doc` regenerates all tables in this document from the committed artifacts.

<!-- table:stamp (generated by `just perf-doc` — edit the artifact, not this block) -->
| instrument | artifact | measured at (git sha) | date |
|---|---|---|---|
| ingest-to-pixel (`just perf-i2p`) | `docs/perf/i2p-baseline.json` | `27deca2` | 2026-08-10T17:50:45Z |
| stage benches (`just bench-baseline`) | `docs/perf/bench-baseline.json` | `27deca2` | 2026-08-10 |
| load scenarios (`just load`) | `docs/perf/load-baseline.json` | `27deca2` | 2026-08-10T17:52:37Z |
| referencer (`just perf-referencer`) | `docs/perf/referencer-baseline.json` | `27deca2` | 2026-08-10 |
<!-- /table:stamp -->

## 1. Method, before numbers

1. **One recipe per instrument.** Each class of measurement has exactly one entry point
   (`just perf-i2p`, `just bench-baseline`, `just load`, `just perf-referencer`). What CI or a
   reviewer runs is what produced the tables here — there is no separate "benchmark script for the
   docs".
2. **Committed artifacts are the ground truth.** The recipes distill raw output into JSON under
   `docs/perf/`. A PR that changes a number changes a committed artifact, so the diff *is* the
   claim review. The tables in this document are generated from those artifacts by `just perf-doc`;
   the prose is hand-written.
3. **Enforcement over aspiration.** The headline number (ingest-to-pixel) is not just measured —
   the same typed e2e harness that measures it asserts it under a budget on every commit
   (`crates/swath-e2e`, `I2P_BUDGET_MS`). The load and bench baselines are regression evidence:
   re-run, diff, explain movement in the PR.

## 2. Environment (full disclosure)

All numbers in this document were captured on one developer laptop:

| | |
|---|---|
| Hardware | Apple M2 Max (MacBook Pro), 12 logical cores (8 performance + 4 efficiency), 32 GB RAM |
| OS | macOS 26.5.2 (Darwin 25.5.0), arm64 |
| Rust | rustc 1.97.1 (cargo 1.97.1); criterion benches use the default `bench` profile (optimized); e2e/load hit the release binary |
| Container stack | Docker Engine 29.3.1, Docker Compose v5.1.1; the served binary is `cargo build --release` inside `rust:1.97.1-slim-trixie`, running in a `debian:trixie-slim` linux/arm64 container (see `Dockerfile`) |
| Load generator | oha 1.15.0 (version-pinned in the `justfile`), plus a unique-URL Python driver for the cold burst (`tests/load/load.py`) |
| Python | Python 3.13.5 via uv 0.12.3; sidecar pins from `python/uv.lock` (virtualizarr 2.7.3, h5py 3.16.0) |
| Serve dataset | The committed HLS fixtures (`tests/fixtures/`): 512×512-pixel windows of one real HLSS30 v2.0 granule (T13SDD, 2024-158), 5 bands, ~1.4 MB total, with real swath-edge nodata — checksummed, byte-reproducible |
| Referencer dataset | The pinned VNP09GA conformance granule `VNP09GA.A2012019.h33v12.002.2023122182434.h5` (8,202,915 bytes, HDF-EOS5, 67 datasets, 1,551 chunk refs — ADR 0008), fetched locally via Earthdata credentials |

Not controlled: thermal state, background processes, page-cache contents. "Cold" below means
"first run in the recipe invocation", not cold-boot. This is a laptop, not a lab.

## 3. Instruments: which recipe, which artifact

| figure | measured by | methodology | committed artifact |
|---|---|---|---|
| Ingest-to-pixel | `just perf-i2p` (runs `just e2e`) | Typed Rust harness (`crates/swath-e2e`) against the full compose stack: asserts an honest 404 while the catalog is empty, drops the fixture granule into the watch dir, polls the north-star tile live, reads `ingest_to_pixel_ms` from the render trace, and pdiff-matches the tile against committed goldens (the number only counts if the pixels are correct) | `docs/perf/i2p-baseline.json` |
| Render-stage benches | `just bench-baseline` (runs `just bench`) | Criterion microbenches over the committed HLS fixtures — planner (`swath-core`) plus render stages (`swath-render`): source window read, warp, IR eval, PNG encode, full-tile composite. No network. Median + MAD (ns) distilled per bench | `docs/perf/bench-baseline.json` |
| Concurrency / load | `just load` | Same compose stack as e2e (`tests/e2e/stack-up.sh`), then pinned scenarios (`tests/load/load.sh`, parameters in `tests/load/load.py`): (a) hot-cache tile storm, (b) cold live-render burst (unique tiles, each exactly once), (c) mixed storm of the heaviest live warps while a second oha measures `/healthz` and curl holds an SSE `/traces` subscription | `docs/perf/load-baseline.json` (+ `.md` rendering) |
| Referencer latency | `just perf-referencer` | Production Rust generator (release `swath ingest reference`) vs the VirtualiZarr sidecar (`python/sidecars/referencer`) on the pinned VNP09GA granule; full-process wall clock, run 1 = cold, warm = median of the remaining 9 runs | `docs/perf/referencer-baseline.json` |

## 4. Ingest-to-pixel (the headline)

The north-star metric (REQUIREMENTS §3): time from *"a new granule arrives"* to *"a correct tile is
visible on the map."* Measured end to end through the real stack — filedrop event, STAC
registration, first live render of a never-before-seen tile — by the same harness that verifies the
tile's pixels against goldens.

<!-- table:i2p (generated by `just perf-doc` — edit the artifact, not this block) -->
| metric | measured | enforced budget |
|---|---:|---:|
| ingest_to_pixel_ms | 646 ms | 10000 ms |
<!-- /table:i2p -->

**Enforcement.** The number is not a brochure figure: `just e2e` asserts `ingest_to_pixel_ms` under
the 10,000 ms budget on every commit (`crates/swath-e2e/src/main.rs`, `I2P_BUDGET_MS`), and CI runs
that harness. The budget is deliberately ~20× above measured values so it catches structural
regressions (an accidental poll interval, a batch step) rather than runner noise; tightening it is a
deliberate, visible act. Context from prior runs of the same harness: 297 ms and 801 ms locally,
535 ms in CI.

Single-request tile latency under no load is visible in the load table below (cold live renders:
p50 ~660 ms for z15 tiles; hot cache hits: p50 ~23 ms at c=32 — see §6).

## 5. Render-stage benches

Where a tile's time goes, stage by stage, on the committed HLS fixtures (criterion, median ± MAD):

<!-- table:bench (generated by `just perf-doc` — edit the artifact, not this block) -->
| bench | median | MAD |
|---|---:|---:|
| composite/render_tile_ndvi_z12 | 37.20 ms | 121.98 us |
| encode_png_ndvi_256 | 13.31 ms | 215.64 us |
| eval_ndvi_grayscale | 3.33 ms | 88.64 us |
| eval_ndvi_rdylgn | 3.39 ms | 24.13 us |
| plan_cache_hit | 52.9 ns | 0.3 ns |
| plan_miss_three_bands | 70.0 ns | 0.2 ns |
| plan_overview_single_band | 43.9 ns | 0.1 ns |
| source_window_z12 | 13.77 us | 129.1 ns |
| warp_bilinear_fullres_z12 | 8.85 ms | 13.66 us |
| warp_nearest_fullres_z12 | 10.04 ms | 2.28 ms |
<!-- /table:bench -->

Reading it: planning is free (tens of ns), reading the source window is ~13 µs, the expensive
stages are the warp (~9–10 ms), NDVI expression eval (~3.3 ms), and PNG encode (~13 ms); a full
NDVI tile composite is ~37 ms of pure compute. Compare any re-run against
`docs/perf/bench-baseline.json`; a PR that moves these should say why.

## 6. Concurrency and the async-vs-blocking boundary

`just load` is the concurrency measurement the e2e never takes (ARCHITECTURE §16.7). All scenarios
run against the containerized release binary over loopback HTTP:

<!-- table:load (generated by `just perf-doc` — edit the artifact, not this block) -->
| scenario | requests | errors | rps | p50 ms | p95 ms | p99 ms | max ms |
|---|---:|---:|---:|---:|---:|---:|---:|
| healthz — idle baseline | 84450 | 0 | 16883.2 | 0.23 | 0.3 | 0.36 | 3.84 |
| (a) hot-cache tile storm | 26306 | 0 | 1313.7 | 23.33 | 34.91 | 41.79 | 72.57 |
| (b) cold live-render burst | 128 | 0 | 11.5 | 660.61 | 1140.7 | 1221.21 | 1231.08 |
| (c) mixed tile storm | 813 | 0 | 20.1 | 852.82 | 1983.87 | 2051.72 | 2096.35 |
| (c) healthz UNDER WARPS | 190707 | 0 | 9533.8 | 0.39 | 0.64 | 0.97 | 30.35 |

SSE `/traces` under the mixed storm: survived the 45s window: **yes** (828 trace events received).
<!-- /table:load -->

The §16.7 evidence is the last two rows: while c=16 clients hammer the heaviest full-footprint
live warps (kept on the Live path by a cache-buster), the control plane stays flat — `/healthz`
p99 under warps vs idle is sub-millisecond in both cases, and the SSE `/traces` subscription
survives the whole window. Blocking render work is not starving the async control plane.

## 7. Referencer: the historic "40×" and "14 ms" claims, re-run

**Provenance of the claims.** Prototype 0001 (referencer bake-off, concluded 2026-08-08, immutable)
measured legacy-format virtual-reference generation on the VNP09GA granule: *"referencer-rs 29 ms
cold / 14 ms warm; virtualizarr sidecar 876 ms cold / ~556–574 ms warm … Rust ≤ sidecar by ~40×
warm."* Those numbers came from the prototype's dev-profile harness timing the Rust generator
**in-process** while timing the sidecar **as a subprocess** (Python spawn + import included).

**Re-measurement on this tree.** The prototype code is frozen; what ships is the production
generator (`swath ingest reference`) and the promoted conformance sidecar
(`python/sidecars/referencer`). `just perf-referencer` measures both the same way — full-process
wall clock, release profile, same granule — which is *less* flattering to Rust than the prototype's
in-process timing (the Rust figure now includes process spawn, CLI startup, and writing the 201 KB
manifest):

```bash
just perf-referencer   # fetches the pinned granule if absent (needs Earthdata netrc)
```

<!-- table:referencer (generated by `just perf-doc` — edit the artifact, not this block) -->
| generator | command | cold | warm (median) |
|---|---|---:|---:|
| referencer-rs | `target/release/swath ingest reference <path-to>/VNP09GA.A2012019.h33v12.002.2023122182434.h5 --output target/referencer/perf-rs.vmanifest.json` | 456.2 ms | 13.8 ms |
| virtualizarr-sidecar | `python/.venv/bin/swath-referencer <path-to>/VNP09GA.A2012019.h33v12.002.2023122182434.h5` | 13278.0 ms | 545.6 ms |

Warm-generation ratio (sidecar / Rust): **~39.5x** in Rust's favor (10 runs; full-process wall clock; run 1 = cold, warm = median of the rest).
<!-- /table:referencer -->

**Verdicts, claim by claim** (this run: `docs/perf/referencer-baseline.json`, all 10 raw samples
in the artifact):

- **"14 ms warm" — reproduces.** Warm median 13.8 ms for the production release binary,
  full-process (the prototype's 14 ms was in-process, dev profile — the release-profile speedup
  and the added process overhead happen to cancel).
- **"~40× warm" — reproduces.** Sidecar warm median 545.6 ms / Rust 13.8 ms = **39.5×**, squarely
  in line with the prototype's ~40× (its sidecar range was ~556–574 ms; ours is 535–557 ms).
- **The cold numbers are NOT comparable to the prototype's and we don't headline them.** This
  run's cold figures are first-invocation-after-build effects: the Rust 456 ms first run is
  dominated by first-touch of a freshly linked ~4.5 MB binary plus macOS code-signature
  verification; the sidecar's 13.3 s first run is dominated by one-time bytecode compilation of a
  fresh `uv sync` venv. The prototype's "29 ms cold / 876 ms cold" were first runs against an
  already-exercised binary and venv. Steady-state (warm) is the operationally meaningful figure
  for a per-granule ingest step, and it is the one we claim.

Both generators emit byte-range-equivalent manifests for this granule — that equivalence is the
gated conformance test (`just test-referencer`), so the speed comparison is between two
implementations producing verified-identical output.

**Honest accounting of the ratio.** The sidecar's per-invocation cost is dominated by Python
interpreter + import time, and the production ingest path runs the generator per granule as a
process — so per-invocation is the operationally honest unit. But stated plainly: this is an
architecture result (no interpreter, no warm-up, 4.5 MB static binary vs a ~273 MB venv), not a
claim that Rust scans HDF5 chunk indexes ~40× faster than h5py once Python is already warm.

## 8. What we don't claim

Bounded honesty, in the spirit of the requirements' "reports it continuously and honestly":

- **Fixture scale.** Serve-path numbers are measured over ~1.4 MB of committed HLS fixtures and
  one 8 MB VNP09GA granule. No claims about multi-terabyte catalogs, thousands of layers, or
  granules that dwarf RAM.
- **Single node, loopback.** One container on one laptop, `localhost` HTTP. No network latency, no
  TLS termination, no CDN, no horizontal scaling, no claims about behavior behind a real ingress.
- **Local storage.** Sources and cache are local disk (the e2e stack's MinIO is loopback too). No
  claims about S3 cross-region byte-range latency on the virtual-reference read path.
- **Tested concurrency only.** Load scenarios go up to c=32 for seconds-to-minutes windows. No
  claims about sustained multi-hour load, connection counts beyond those tested, or fairness under
  overload.
- **Microbenches are isolated stages.** Criterion numbers measure each stage alone on a quiet
  machine; they do not include queueing, cache contention, or concurrent-warp interference (that is
  what §6 samples).
- **One machine, uncontrolled.** A developer laptop, thermals and background load not isolated.
  Cross-machine comparisons of absolute numbers are not supported; diff same-machine baselines.
- **The 40× ratio is per-invocation, one granule, one format.** See §7's accounting — it includes
  Python startup by design, and was measured on a single HDF-EOS5 granule.
- **CI runners are slower and noisier.** The enforcement budget (10 s) is sized for that; the
  measured numbers here are laptop numbers.

## 9. Regenerating this document

```bash
just bench-baseline    # re-run all criterion benches, rewrite docs/perf/bench-baseline.json
just load              # bring up the stack, run load scenarios, rewrite docs/perf/load-baseline.{json,md}
just perf-i2p          # run the typed e2e, commit its metrics as docs/perf/i2p-baseline.json
just perf-referencer   # re-measure the referencer bake-off, rewrite docs/perf/referencer-baseline.json
just perf-doc          # regenerate every table above from the committed artifacts
```

Commit the artifact diffs alongside the regenerated tables; the PR should say why numbers moved.
