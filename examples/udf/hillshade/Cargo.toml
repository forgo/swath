# SPDX headers: covered by REUSE.toml aggregate annotation for **/Cargo.toml.
[package]
name = "swath-udf-example-hillshade"
description = "Example Swath UDF (ABI v1): Horn hillshade — a neighborhood op band math cannot express, and the tile-seam caveat made visible."
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
swath-udf-guest = { path = "../../../crates/swath-udf-guest" }
