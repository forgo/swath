# Python sidecar deps for prototype 0001 (the VirtualiZarr reference generator).
# PINNED as run for the recorded bake-off results (README §7, 2026-08-08) so the
# results stay reproducible. The prototype is concluded; do not bump casually.
virtualizarr==2.7.3
h5py==3.16.0          # HDF5 / NetCDF4 (VIIRS VNP09GA)
kerchunk==0.2.10      # GRIB2 reference scanner (scan_grib)
cfgrib==0.9.15.1      # GRIB2 decoding for the reference path
eccodes==2.47.0
xarray==2026.7.0
numpy==2.5.1
# NASA data access for scripts/fetch_sample.sh:
earthaccess==0.18.0
